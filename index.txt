<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <title>Análise de RO - Cebrace</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <style>
    * {
      box-sizing: border-box;
    }

    body {
      font-family: Arial, Helvetica, sans-serif;
      background: #f3f4f6;
      margin: 0;
      padding: 20px;
      color: #111827;
    }

    .container {
      max-width: 1100px;
      margin: 0 auto;
      background: #ffffff;
      padding: 24px 24px 32px;
      border-radius: 12px;
      box-shadow: 0 8px 25px rgba(0,0,0,0.08);
    }

    h1, h2, h3 {
      margin-top: 0;
      color: #111827;
    }

    h1 {
      font-size: 1.8rem;
      margin-bottom: 8px;
    }

    h2 {
      font-size: 1.3rem;
      margin-top: 24px;
      border-left: 4px solid #2563eb;
      padding-left: 8px;
    }

    h3 {
      font-size: 1.1rem;
      margin-top: 16px;
      color: #1f2937;
    }

    .subtitle {
      font-size: 0.9rem;
      color: #6b7280;
      margin-bottom: 16px;
    }

    .grid-2 {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(230px, 1fr));
      gap: 12px 16px;
      margin-bottom: 16px;
    }

    .field {
      display: flex;
      flex-direction: column;
      gap: 4px;
      margin-bottom: 12px;
    }

    label {
      font-size: 0.9rem;
      font-weight: 600;
      color: #374151;
    }

    input[type="text"],
    input[type="number"],
    input[type="date"],
    textarea {
      padding: 8px 10px;
      border-radius: 6px;
      border: 1px solid #d1d5db;
      font-size: 0.9rem;
      outline: none;
      transition: border 0.15s ease, box-shadow 0.15s ease;
      background: #f9fafb;
    }

    input[type="text"]:focus,
    input[type="number"]:focus,
    input[type="date"]:focus,
    textarea:focus {
      border-color: #2563eb;
      box-shadow: 0 0 0 2px rgba(37,99,235,0.15);
      background: #ffffff;
    }

    textarea {
      min-height: 70px;
      resize: vertical;
    }

    input[type="file"] {
      font-size: 0.85rem;
    }

    .hint {
      font-size: 0.75rem;
      color: #9ca3af;
    }

    .section-card {
      border: 1px solid #e5e7eb;
      border-radius: 10px;
      padding: 16px;
      margin-top: 16px;
      background: #f9fafb;
    }

    .section-card-title {
      font-size: 0.9rem;
      font-weight: 700;
      margin-bottom: 8px;
      color: #111827;
    }

    .decision-tree {
      border: 1px dashed #9ca3af;
      border-radius: 8px;
      padding: 12px;
      margin-top: 8px;
      background: #f3f4f6;
      font-size: 0.85rem;
      color: #6b7280;
    }

    .decision-tree-header {
      font-weight: 600;
      margin-bottom: 4px;
    }

    .readonly {
      background: #e5e7eb;
    }

    .buttons {
      display: flex;
      flex-wrap: wrap;
      gap: 12px;
      margin-top: 24px;
    }

    button {
      border: none;
      border-radius: 999px;
      padding: 10px 20px;
      font-size: 0.95rem;
      font-weight: 600;
      cursor: pointer;
      display: inline-flex;
      align-items: center;
      gap: 6px;
      transition: transform 0.1s ease, box-shadow 0.1s ease, background 0.1s ease;
    }

    button:active {
      transform: translateY(1px);
      box-shadow: none;
    }

    .btn-primary {
      background: #2563eb;
      color: #ffffff;
      box-shadow: 0 4px 12px rgba(37, 99, 235, 0.35);
    }

    .btn-primary:hover {
      background: #1d4ed8;
    }

    .btn-secondary {
      background: #10b981;
      color: #ffffff;
      box-shadow: 0 4px 12px rgba(16,185,129,0.35);
    }

    .badge {
      display: inline-block;
      font-size: 0.7rem;
      font-weight: 600;
      padding: 2px 8px;
      border-radius: 999px;
      background: #eff6ff;
      color: #1d4ed8;
      margin-left: 4px;
    }

    @media (max-width: 600px) {
      body {
        padding: 12px;
      }
      .container {
        padding: 16px;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>Análise de RO</h1>
    <div class="subtitle">
      Formulário para registro da análise da Reclamação de Ocorrência (RO) e visita em loco.
    </div>

    <!-- DADOS GERAIS DA VISITA -->
    <h2>Dados da visita / RO</h2>
    <div class="grid-2">
      <div class="field">
        <label for="empresa">Empresa visitada</label>
        <input type="text" id="empresa" placeholder="Razão social / nome fantasia" />
      </div>
      <div class="field">
        <label for="numeroRo">Número da RO</label>
        <input type="text" id="numeroRo" placeholder="Ex: RO-2025-00123" />
      </div>
      <div class="field">
        <label for="dataVisita">Data da visita</label>
        <input type="date" id="dataVisita" />
      </div>
      <div class="field">
        <label for="representanteCebrace">Representante Cebrace</label>
        <input type="text" id="representanteCebrace" placeholder="Nome do representante da Cebrace" />
      </div>
      <div class="field">
        <label for="representanteEmpresa">Representante da empresa</label>
        <input type="text" id="representanteEmpresa" placeholder="Contato do cliente / empresa" />
      </div>
    </div>

    <!-- IMAGEM DA RO -->
    <div class="section-card">
      <div class="section-card-title">Imagem da RO</div>
      <div class="field">
        <label for="imagemRo">Campo para inserir imagem da RO</label>
        <input type="file" id="imagemRo" accept="image/*" />
        <span class="hint">Você pode anexar uma foto ou print da RO.</span>
      </div>
    </div>

    <!-- LOTES RECLAMADOS -->
    <div class="section-card">
      <div class="section-card-title">Lotes Reclamados</div>
      <div class="field">
        <label for="obsLotes">Observações sobre os lotes reclamados</label>
        <textarea id="obsLotes" placeholder="Descreva os lotes, códigos, quantidades, etc."></textarea>
      </div>
      <div class="field">
        <label for="fotoLotes">Fotos dos lotes reclamados</label>
        <input type="file" id="fotoLotes" accept="image/*" multiple />
        <span class="hint">Permite anexar mais de uma foto.</span>
      </div>
    </div>

    <!-- EVIDÊNCIAS ANEXAS -->
    <div class="section-card">
      <div class="section-card-title">Evidências anexas no RO</div>
      <div class="field">
        <label for="fotosEvidencias">Fotos / evidências anexas no RO</label>
        <input type="file" id="fotosEvidencias" accept="image/*" multiple />
      </div>
    </div>

    <!-- FOTOS DAS AMOSTRAS RECEBIDAS -->
    <div class="section-card">
      <div class="section-card-title">Fotos das amostras recebidas</div>
      <div class="field">
        <label for="fotosAmostras">Campo de inserir fotos das amostras recebidas</label>
        <input type="file" id="fotosAmostras" accept="image/*" multiple />
      </div>
    </div>

    <!-- RESULTADOS DE BUSCA BINÁRIA -->
    <div class="section-card">
      <div class="section-card-title">Resultados de busca binária</div>
      <div class="field">
        <label for="fotosBuscaBinaria">Fotos dos resultados de busca binária</label>
        <input type="file" id="fotosBuscaBinaria" accept="image/*" multiple />
      </div>
    </div>

    <!-- AMOSTRA DE PILHA -->
    <div class="section-card">
      <div class="section-card-title">Amostra de pilha</div>
      <div class="field">
        <label for="fotosPilha">Fotos da amostra de pilha</label>
        <input type="file" id="fotosPilha" accept="image/*" multiple />
      </div>
    </div>

    <!-- VISITA REALIZADA EM LOCO -->
    <h2>Visita realizada em loco</h2>

    <h3>Inspeção</h3>
    <div class="grid-2">
      <div class="field">
        <label for="racksSegregados">Quantidade total de racks segregados</label>
        <input type="number" id="racksSegregados" min="0" />
      </div>
      <div class="field">
        <label for="totalPecas">Quantidade total de peças</label>
        <input type="number" id="totalPecas" min="0" />
      </div>
      <div class="field">
        <label for="dimensaoPecas">Dimensão das peças</label>
        <input type="text" id="dimensaoPecas" placeholder="Ex: 3210 x 2250 mm" />
      </div>
      <div class="field">
        <label for="amostragemInspecionada">Amostragem inspecionada</label>
        <input type="text" id="amostragemInspecionada" placeholder="Ex: 20% das peças / 3 racks" />
      </div>
    </div>

    <!-- RESULTADO DA AMOSTRAGEM -->
    <h3>Resultado da amostragem</h3>

    <div class="grid-2">
      <div class="field">
        <label for="pecasComDefeito">Peças com defeito</label>
        <input type="number" id="pecasComDefeito" min="0" />
      </div>
      <div class="field">
        <label for="pecasSemDefeito">Peças sem defeito</label>
        <input type="number" id="pecasSemDefeito" min="0" />
      </div>
    </div>

    <!-- ÁRVORE DE DECISÃO - FORMATO APENAS -->
    <div class="section-card">
      <div class="section-card-title">
        Árvore de decisão dos defeitos
        <span class="badge">formato</span>
      </div>

      <!-- Peças com defeito -->
      <div class="decision-tree">
        <div class="decision-tree-header">Peças com defeito</div>
        <!-- Espaço reservado SOMENTE PARA FORMATO (sem itens por enquanto) -->
        <div class="hint">
          Espaço reservado para futura árvore de decisão dos defeitos identificados (não preencher ainda).
        </div>
      </div>

      <!-- Peças sem defeito -->
      <div class="decision-tree" style="margin-top: 8px;">
        <div class="decision-tree-header">Peças sem defeito</div>
        <!-- Espaço reservado SOMENTE PARA FORMATO (sem itens por enquanto) -->
        <div class="hint">
          Espaço reservado para futura árvore de decisão para peças sem defeito (não preencher ainda).
        </div>
      </div>
    </div>

    <!-- PERCENTUAIS -->
    <div class="grid-2" style="margin-top: 16px;">
      <div class="field">
        <label for="percentProcedente">% procedente</label>
        <input
          type="text"
          id="percentProcedente"
          class="readonly"
          placeholder="Calculado automaticamente"
          readonly
        />
        <span class="hint">Baseado em peças com defeito / total de peças inspecionadas.</span>
      </div>
      <div class="field">
        <label for="percentNaoProcedente">% não procedente</label>
        <input
          type="text"
          id="percentNaoProcedente"
          class="readonly"
          placeholder="Calculado automaticamente"
          readonly
        />
        <span class="hint">Baseado em peças sem defeito / total de peças inspecionadas.</span>
      </div>
    </div>

    <!-- PEÇAS COM DEFEITO IDENTIFICADAS NA VISITA -->
    <div class="section-card">
      <div class="section-card-title">Peças com defeito identificadas na visita</div>
      <div class="field">
        <label for="fotosPecasDefeito">Fotos das peças com defeito</label>
        <input type="file" id="fotosPecasDefeito" accept="image/*" multiple />
      </div>
    </div>

    <!-- CONCLUSÃO -->
    <h2>Conclusão</h2>
    <div class="field">
      <label for="conclusaoObs">Observações / conclusão da análise</label>
      <textarea
        id="conclusaoObs"
        placeholder="Descreva a conclusão da visita, se a reclamação é procedente, ações recomendadas, etc."
      ></textarea>
    </div>

    <!-- BOTÕES -->
    <div class="buttons">
      <button type="button" class="btn-primary" onclick="registrarDados()">
        💾 Registrar
      </button>
      <button type="button" class="btn-secondary" onclick="exportarPPT()">
        📊 Exportar para PPT
      </button>
    </div>
  </div>

  <script>
    // Cálculo automático de % procedente e % não procedente (formato básico)
    const inputComDefeito = document.getElementById('pecasComDefeito');
    const inputSemDefeito = document.getElementById('pecasSemDefeito');
    const outputProcedente = document.getElementById('percentProcedente');
    const outputNaoProcedente = document.getElementById('percentNaoProcedente');

    function atualizarPercentuais() {
      const comDefeito = Number(inputComDefeito.value) || 0;
      const semDefeito = Number(inputSemDefeito.value) || 0;
      const total = comDefeito + semDefeito;

      if (total > 0) {
        const pProcedente = (comDefeito / total) * 100;
        const pNaoProcedente = (semDefeito / total) * 100;

        outputProcedente.value = pProcedente.toFixed(2) + ' %';
        outputNaoProcedente.value = pNaoProcedente.toFixed(2) + ' %';
      } else {
        outputProcedente.value = '';
        outputNaoProcedente.value = '';
      }
    }

    inputComDefeito.addEventListener('input', atualizarPercentuais);
    inputSemDefeito.addEventListener('input', atualizarPercentuais);

    // Botão "Registrar" - por enquanto apenas um alerta (formato)
    function registrarDados() {
      alert('Formato de registro criado. Aqui depois você pode salvar em banco de dados / planilha.');
    }

    // Botão "Exportar para PPT" - formato / placeholder
    function exportarPPT() {
      alert(
        'Formato do botão de exportação para PPT criado.\n\nPróximo passo: implementar a geração automática de slides usando esses campos e imagens.'
      );
    }
  </script>
</body>
</html>
